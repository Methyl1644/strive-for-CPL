#include <stdio.h>

#define M 3
#define N 10
/*int main() {
    int n;
    int a[n];//variable-length array;不允许这么写！！

    char array[M][N];
    for (int r = 0;r < M;r++) {
        scanf("%s",array[r]);
    }//scanf a row
    //array[0] is int a[10]
    scanf("%9s",array);//因为最后一个是'\0'

    printf("%s\n",array);

    printf("%lu %lu %lu",sizeof(array),sizeof(array[0]));
}*/
int